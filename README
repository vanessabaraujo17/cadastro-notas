# Dependencias
É necessario ter `docker` e `docker-compose` instalados, caso não possua verifique as documentações abaixo
 - [docker](https://docs.docker.com/engine/install/)
 - [docker-compose](https://docs.docker.com/compose/install/)

# Executando
Para executar digite:
```
docker-compose up -d
```

Para encerrar digite:
```
docker-compose down
```

